# Public Data Conversion Tools

This package contains portable converters for external solubility datasets. Each command exposes its input schema through `--help` and writes standardized, model-ready tables without embedding machine-specific paths.
