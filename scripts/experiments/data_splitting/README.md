# Data-Splitting Experiments

`run_kfold_cv.py` constructs system-level cross-validation folds. `run_split_strategy_benchmark.py` compares point-level, system-level, stratified, and structure-family split strategies.
