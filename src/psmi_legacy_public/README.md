# 历史公开权重兼容包

本包保留加载早期公开检查点所需的模型、数据、特征和配置代码，与当前维护的
`src/psmi/` 严格分离。

历史权重属于两标量模型，并包含旧版批处理行为。除非明确复现历史实验，否则新训练
不得导入本包，也不得把历史指标标记为 `corrected_v2`。

- `model.py`：历史 `LLEGraphNet`；
- `data.py`：历史图数据集和批处理；
- `utils.py`：配套分子特征；
- `config.py`：兼容配置；
- `paths.py`：仓库相对路径。

兼容层只保证旧权重可加载，不代表旧实现具备当前论文声称的全部机制。
