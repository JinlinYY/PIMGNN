# PSMI Baseline Models

This package contains the public baseline implementations used by the experiment wrappers: classical neural models, CIGIN, CGIB, GLAM, MMGNN, SolvBERT, and external-solubility predictors. Shared split and metric utilities are located in `common/` and `protocol.py`.
