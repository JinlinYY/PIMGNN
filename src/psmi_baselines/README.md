# 对比模型源码

本包保存通用模型、CGIB、CIGNN、GLAM、MMGNN、SolvBERT 和 BigSolvDB 外部验证
流程。当前 PSMI 主模型位于 `src/psmi/`，本目录中的模型仅用于受控对比。

共享路径集中在 `paths.py`，固定划分校验位于 `protocol.py`。修正版入口统一使用
612/75/78 个体系的主基准清单，并禁止训练期间用测试指标选模。

导入代码尽量保留原始架构，但规范了包导入、输出位置、英文代码注释和命令行默认
路径。缺少完整数据、配置或检查点来源的历史结果只能归档，必须重新运行后才可进入
修正版论文表格。
