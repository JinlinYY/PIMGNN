# Baseline Comparison Dataset

This directory provides the fixed train, validation, and test partitions used by the public baseline interfaces. `split_manifest.csv` records the system-level assignment, and `total.csv` contains the combined table with an explicit `split` column.
